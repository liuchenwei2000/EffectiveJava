                                                                  用实例域代替序数


所有的枚举都有一个 ordinal 方法，用来返回每个枚举常量所关联的 int 值（序数）。
但是永远不要基于枚举的序数导出与它关联的值，而是要将值保存在一个实例域中。
ordinal 方法是设计成用于像 EnumSet 或 EnumMap 这种基于枚举的通用数据结构的，除非编写这类数据结构，否则最好避免使用该方法。