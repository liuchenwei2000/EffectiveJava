                                                             始终要覆盖toString

Object提供了toString方法的一个实现：它包含类的名称，以及一个“@”符号，接着是散列码的无符号十六进制表示。

toString的通用约定指出，被返回的字符串应该是一个简洁的、信息丰富的并且易于阅读的表达形式，建议所有的子类都要改写。

在实际应用中，toString方法应该返回对象中包含的所有值得关注的信息。
